# Copy thành .env rồi điền. KHÔNG commit .env
LLM_PROVIDER=openai      # openai | gemini | mock (mock = không gọi AI thật, chỉ để test offline)
LLM_MODEL=gpt-4o-mini    # tên model của nhà cung cấp đã chọn
LLM_API_KEY=
